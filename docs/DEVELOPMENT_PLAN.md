# DEVELOPMENT PLAN

## 開発原則

各マイルストーンは、独立して起動・テストできる状態で完了させる。UI追加より先に事件データと判定条件を固定し、証拠なしに解ける抜け道がないか確認する。

## ロードマップ

| Milestone | ゴール | 完了条件 |
| --- | --- | --- |
| M01 | Playable Investigation Prototype | HOTEL DB検索、0件表示、結果の証拠化 |
| M02 | Multiple Data Sources | 6ソースの操作差と共通結果型 |
| M03 | Evidence System | 発見条件、詳細表示、永続化 |
| M04 | Case Board | 証拠ノード接続、矛盾・関係のラベル |
| M05 | Story Layer | 導入、通信、節目イベント、ログ |
| M06 | Case Theory | 事件モデル編集、証拠割当、提出 |
| M07 | CASE 001 Complete | 真相まで通しプレイ、難易度調整 |
| M08 | Presentation | 音響、画面演出、アクセシビリティ |
| M09 | CASE 002 Pipeline | データ差し替えで章追加可能 |

## M01 現在地

- [x] Next.js / TypeScript 雛形
- [x] 捜査端末のレスポンシブUI
- [x] CASE 001 HOTEL DB サンプル
- [x] 限定SQL風クエリエンジン
- [x] 0件を手掛かりとして表示
- [x] 行から証拠を登録
- [x] クエリエンジン単体テスト
- [x] 実ブラウザでの操作確認（Chromium、390px / 1440px、M02追加時にM01を含め確認）
- [ ] GitHubリポジトリ作成とCI

## M02 現在地（6ソース対応）

- [x] `DataSourceAdapter` と共通結果・証拠候補型
- [x] HOTEL DBをHotelAdapterへ移行（既存hotel.jsonは変更なし）
- [x] ACCESS DBの独立JSONと限定検索式（AND、完全一致、timestamp範囲、time別名）
- [x] FACILITY DBの独立JSONと部屋・時刻範囲フォーム、電力時系列グラフ
- [x] 全6ソースの結果行から既存Evidenceへ登録
- [x] ソース・テーブル・レコードIDによる重複防止（HOTELの列選択後も同一ID）
- [x] モバイルでのソース切替と結果表の横スクロール
- [x] 単体テスト71件成功、`npm run build`成功
- [x] Chromiumの390px / 1440pxで0件検索・3ソース切替・証拠登録・重複防止・グラフの0/1/複数件・不正入力・ページ幅を確認（一時スモークスクリプト）
- [x] CAMERAのカメラ・人物タグ・時間帯検索と映像メタデータのタイムライン
- [x] PAYMENTの端末・決済手段・金額範囲フィルター
- [x] STAFFの人物→部署・権限→関連人物の探索
- [x] Playwrightの回帰テストをリポジトリに常設（`npm run build`後に`npm run test:e2e`）
- [x] 常設E2Eが390px / 1440pxの両方で成功（6ソース検索、証拠登録、関係探索、入力エラー、横幅）
- [ ] GitHub Actionsへの回帰テスト組み込み

追加データは事件当日2026-10-14、UTC+09:00のサンプル記録。真相や犯人の判定は追加していない。時間範囲は両端の分を含み、日付をまたぐ検索は未対応。

M02後半で既存HOTEL / ACCESS / FACILITYのレコードは変更していない。

## M03 現在地

- [x] `evidence.json`による名称・タグ・発見条件（recordReturned）の定義
- [x] 検索結果の不変IDで発見を判定し、未発見レコードの登録を防止
- [x] 証拠詳細で元レコード全体を表示（列選択した結果からの登録も対応）
- [x] 発見済みID・登録済みID・最新100件の成功した検索履歴をlocalStorageに保存・復元
- [x] 事件・保存形式のバージョン検証、未知ID・破損データの拒否、既存保存の上書き防止
- [x] 保存不能時の状態表示とメモリ上での継続操作
- [x] 単体テスト87件、E2E 8件（390px / 1440px）、ビルド成功

M03では既存6ソースの事件レコードを変更していない。保存形式はv1から開始し、旧永続化形式は存在しない。複数タブの競合解決・0件結果の証拠化・入力途中の復元は未対応。

## M04 現在地

- [x] 登録済み証拠のボード配置、移動、取り外し
- [x] 関係／矛盾の無向接続、200文字のメモ、編集・解除
- [x] 未登録証拠・自己接続・重複接続・不正座標の検証
- [x] 配置の取り外し時に接続だけを整理し、証拠登録は維持
- [x] ボードを含む保存v2とM03保存v1からの移行
- [x] モバイルの内部スクロールとキーボード操作
- [x] 単体103件、E2E 10件（390px / 1440px）、ビルド成功

既存事件データは変更なし。接続はプレイヤーの仮説であり、真相の判定や自動接続は行わない。移動はボタン式の3列×20行グリッドで、ドラッグ移動・自由な人物ノード作成は未対応。

## M05 現在地

- [x] `story.json`による導入・通信5件・目標・節目条件の定義
- [x] 開始・発見・証拠登録・ボード接続の条件を純粋関数で判定
- [x] 受信順の通信ログと未読数・既読操作、重複受信防止
- [x] 条件成立による目標更新、遅れて発見した手掛かりによる目標後退の防止
- [x] 保存v3に受信・既読IDを追加し、v1・v2から移行
- [x] 単体115件、E2E 12件（390px / 1440px）、型チェック・ビルド成功

既存6ソースの記録と事件設定は変更なし。通信は捜査支援の文面で、犯人・真相・仮説の正誤判定は含まない。音声・通信の割り込み演出は未実装。

## M06 現在地

- [x] 犯人・動機・方法・死亡時刻の自由記述と根拠説明
- [x] 各項目への登録済み証拠の割当・解除
- [x] 記入漏れ・証拠不足の検証、提出前確認、ローカル提出履歴
- [x] 提出時点の内容を下書きと独立して保持、直前の重複提出防止
- [x] 下書き・提出履歴を保存v4へ追加、v1〜v3から移行
- [x] 単体125件、E2E 14件（390px / 1440px）、ビルド成功

M06時点では既存事件記録は変更なし。提出はブラウザ内の記録で、採点・正誤判定・エンディングは未実装。正解が未確定の項目を推測してUIへ埋め込まない。M07で承認済みの追加設定と評価を実装した。

M07で動機・方法・死亡時刻の正解と根拠の対応を設計し、承認済みの追加事件データとサーバー側評価を実装した。

## M07 現在地（通しプレイ実装済み）

- [x] 現行証拠の監査と、権限・電力・映像タグだけでは確定できない結論の整理
- [x] `CASE_001_RESOLUTION_PLAN.md`に追加設定、8件の追加証拠、項目別の必須根拠、API・保存の実装方針を具体化
- [x] 結論IDと必要証拠群の両方を要求する汎用採点コア
- [x] 合成データで根拠不足・誤答・未知ID・未登録証拠・答えの非開示を検証
- [x] ユーザー承認を受け、新規の動機・死亡時間帯・追加証拠を採用
- [x] 独立JSONの8追加記録、4アダプター・共通証拠カタログへの合流
- [x] サーバー専用正解表とserver-only境界
- [x] 評価用結論・評価API・評価履歴・解決結果の接続
- [x] 保存v5、v1〜v4の移行、旧提出の保持
- [x] 誤答／証拠不足／全項目支持、通信失敗からの再試行
- [x] 単体144件、E2E 18件、390px / 1440pxの証拠検索から解決までの通しプレイ、ビルド成功
- [x] クライアント成果物に正解表・解決文が混入していないことを確認
- [ ] 初見プレイヤーの通しプレイと難易度・証拠文面の調整

既存6ソースのレコードは変更なし。新しい動機・死亡推定時間帯は明示的承認済み。機能上はCASE 001の解決まで到達可能。試遊版のlocalStorage進捗は改ざん可能で、サーバーが発見過程を保証する仕組みはない。人による45〜75分の難易度検証は未実施。

初見プレイによる難易度調整は未実施。`PLAYTEST_GUIDE.md`に実施手順と記録票を用意した。

## M08 現在地（表示・操作支援・効果音）

- [x] native dialogによる事件概要の背景操作抑止、初期フォーカス、Esc閉鎖、操作位置の復帰
- [x] データ検索・証拠・通信・ボード・事件モデルへのキーボード対応移動リンク
- [x] 検索結果件数のライブ通知とフォーカス表示
- [x] 概要の高さ制限・内部スクロール、空表示の文字色改善
- [x] 概要・判定結果の短い表示演出とprefers-reduced-motion対応
- [x] 単体144件、E2E 22件、ビルド成功。390px／1440px、高さ320px、キーボード操作、動き抑制を検証
- [ ] スクリーンリーダー実機確認と全操作のアクセシビリティ監査
- [x] 明示的なオン操作で開始する検索・証拠登録・通信受信の効果音
- [x] ミュート／音量／試聴、音量のみの独立保存、初期無音、背景タブ停止
- [x] 音声利用不可時の捜査継続、再生の重複抑止、復元済み通信の再通知防止
- [x] 単体151件、E2E 26件、ビルド成功。390px／1440pxで音響操作・音量復元・音声利用不可時の捜査継続を確認
- [ ] 初見3人の試遊・重要証拠発見率・所要時間の測定

事件記録・正解表・捜査保存形式は変更なし。音量は独立キー `null-case:audio-volume:v1` に保存する。BGM・音声台詞は未実装。実機での聴感・スクリーンリーダー確認、初見試遊は未実施。次はアクセシビリティ監査と試遊結果の収集を進める。

## M02 実装順

1. `DataSourceAdapter` インターフェースを定義
2. HOTEL DBをアダプターへ移行
3. ACCESS DBとFACILITY DBの事件データを追加
4. ログ検索式と時系列UIを実装
5. CAMERA / PAYMENT / STAFFを追加
6. すべての結果を共通の証拠候補形式へ変換
7. Playwrightで主要導線を自動化

## GitHub Actions

最初のCIは `npm ci`、`npm test`、`npm run build` の3つだけにする。CASE 001完成後にPlaywrightとアクセシビリティ検査を追加する。

## リリース判定

- 初見プレイヤー3人がヒント込みで完走できる
- 重要証拠の発見率が80%以上
- 主要画面が幅390pxと1440pxで破綻しない
- キーボードだけで検索・証拠化・提出ができる
- 推理の正誤理由をプレイヤーが説明できる
